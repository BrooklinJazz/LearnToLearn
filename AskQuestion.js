const fs = require('fs');
const DesignPatterns = require("./DesignPatterns")
const Elixir = require("./DesignPatterns")
const askQuestion = () => {
  // select question
  // ask for description
  // print description and ask if it matches users
  // if yes - place question in box++
  // if no place question in box --
  // request kata completion if their answer is no
}